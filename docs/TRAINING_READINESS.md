# 机器学习数据资格

`training_readiness.json` 把数据健康状态转换成固定训练计划。默认门槛为：

- 训练窗口至少 120 天；
- 验证窗口 30 天；
- 锁定 OOS 窗口 30 天；
- 覆盖率不低于 98%，并且没有内部缺口。

达到门槛只表示可以开始训练，并不表示完成六个月 OOS。当前 1 分钟短样本和有缺口的口径
会继续保留并显示补数原因，不会阻塞其他成熟频率先进入研究。
